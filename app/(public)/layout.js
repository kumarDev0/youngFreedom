export const metadata = {
  title: 'Application status — YoungFreedom',
  robots: { index: false, follow: false }   // private links stay out of search
};

export default function PublicLayout({ children }) {
  return children;
}
